---
import { ANALYTICS } from '~/utils/config';
---

{
    ANALYTICS?.vendors?.umami?.id ? (
      <script is:inline defer src={ANALYTICS.vendors.umami.url} data-website-id={ANALYTICS.vendors.umami.id}></script>
    ) : null
  }