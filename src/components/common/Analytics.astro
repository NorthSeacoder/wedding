---
import {Umami} from 'astro-analytics';
import { ANALYTICS } from '~/utils/config';
---

{
    ANALYTICS?.vendors?.umami?.id ? (
      <Umami dataId={String(ANALYTICS.vendors.umami.id)} site={ANALYTICS.vendors.umami.url} host='/script.js' />
    ) : null
  }